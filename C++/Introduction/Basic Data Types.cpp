#include <iostream>
#include <cstdio>
using namespace std;

int main() {
    // Complete the code.
    int a; 
    long b;
    float c;
    double d;
    char ch;
    scanf("%d %ld %c %f %lf", &a, &b, &ch, &c, &d);
    printf("%d\n%ld\n%c\n%f\n%lf", a, b, ch, c, d);
    return 0;
}